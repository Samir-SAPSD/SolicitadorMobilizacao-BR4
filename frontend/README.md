# Front-end

This repository already keeps the user interface separated from the server logic in `templates/index.html` and `static/`.
The API layer now lives under `api/`, and the business logic lives under `backend/`.
